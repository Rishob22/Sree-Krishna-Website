const express = require("express");
const {} = require("../controllers/slot.js");
const slotRoute = express.Router();
// slotRoute.post("/get", getSlots);
// slotRoute.post("/:id/hold", holdSlot);
// slotRoute.post("/:id/confirm", confirmSlot);
// slotRoute.post("/:id/cancel", cancelSlot);
module.exports = slotRoute;
